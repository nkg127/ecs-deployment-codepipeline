version: 0.2

phases:
  install:
    commands:
       # Nothing to install.....
       
  pre_build:
      commands:
       #echo "Logging in to Amazon ECR..."
      - aws ecr get-login-password --region us-east-1 docker login --username AWS --password-stdin 334902933163.dkr.ecr.us-east-1.amazonaws.com
  build:
    commands:
      - docker build --tag $REPOSITORY_URI:latest . 
      

  post_build:
    commands:
      - docker push $REPOSITORY_URI:latest 
      - echo Writing image definitions file...
      - printf '{"ImageURI":"%s"}' $REPOSITORY_URI:latest > imagedefinitions.json
artifacts:
    files: imagedefinitions.json
